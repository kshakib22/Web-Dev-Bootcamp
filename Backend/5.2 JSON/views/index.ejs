<!DOCTYPE html>
<html>
  <head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
  </head>

  <body>
    <h1>Taco Town 🌮</h1>

    <form action="/recipe" method="POST" class="buttons">
      <button type="submit" value="chicken" name="choice">🍗</button>
      <button type="submit" value="beef" name="choice">🥩</button>
      <button type="submit" value="fish" name="choice">🐟</button>
    </form>
    <div id="recipeContainer" class="hidden">
      <!-- If no recipe yet, show: -->
      <% if (!locals.name) { %>
      <h2>Pick your favourite taco ingredient 👆</h2>
      <% } else{ %>
      <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
      <h2><%= locals.name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%= locals.ingredients.protein.name %>, <%=
          locals.ingredients.protein.preparation %>
        </li>
        <li><%= locals.ingredients.salsa.name %></li>
        <% for( let index = 0; index < locals.ingredients.toppings.length;
        index++ ) { %>
        <li>
          <%= locals.ingredients.toppings[index].quantity %> of <%=
          locals.ingredients.toppings[index].name %>
        </li>
        <% } %>
      </ul>
      <% } %>
    </div>
  </body>
</html>
